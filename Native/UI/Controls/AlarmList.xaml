<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="mobileclock::ui::controls::AlarmList"
             xmlns="urn:mobileclock:xaml"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer id="alarmsScrollViewer"
                  verticalAlignment="Stretch"
                  horizontalAlignment="Stretch"
                  verticalScrollBarVisibility="Auto">
        <ListView id="alarms"
                  verticalAlignment="Stretch"
                  horizontalAlignment="Stretch"
                  itemsSource="{Binding ItemsSource}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border id="alarmItem"
                            height="200"
                            margin="0 0 16 0"
                            verticalAlignment="Stretch"
                            horizontalAlignment="Stretch">
                        <Border id="alarmBlock"
                                command="{Binding AlarmBlockCommand}"
                                verticalAlignment="Stretch"
                                horizontalAlignment="Stretch">
                            <Grid verticalAlignment="Stretch"
                                  horizontalAlignment="Stretch">
                                <columnDefinitions>
                                    <columnDefinition width="112"/>
                                    <columnDefinition width="80"/>
                                    <columnDefinition width="*"/>
                                </columnDefinitions>
                                <TextBlock gridColumn="0"
                                           verticalAlignment="Center"
                                           horizontalAlignment="Right"
                                           text="{Binding Time}"
                                           fontSize="48"
                                           foreground="#F1E8D2"/>
                                <Border gridColumn="1"
                                        width="36"
                                        height="36"
                                        verticalAlignment="Center"
                                        horizontalAlignment="Center"
                                        background="#171813"
                                        borderBrush="#DDB54F"
                                        borderThickness="8"
                                        cornerRadius="18"/>
                                <Grid gridColumn="2"
                                      margin="0 0 18 18"
                                      verticalAlignment="Stretch"
                                      horizontalAlignment="Stretch">
                                    <Border background="#22231B"
                                            borderBrush="#292A20"
                                            borderThickness="1"
                                            cornerRadius="22"
                                            verticalAlignment="Stretch"
                                            horizontalAlignment="Stretch"/>
                                    <Grid margin="30 28 24 24"
                                          verticalAlignment="Stretch"
                                          horizontalAlignment="Stretch">
                                        <columnDefinitions>
                                            <columnDefinition width="*"/>
                                            <columnDefinition width="84"/>
                                        </columnDefinitions>
                                        <StackPanel verticalAlignment="Center"
                                                    horizontalAlignment="Left"
                                                    orientation="Vertical">
                                            <Border width="134"
                                                    height="14"
                                                    margin="0 0 0 30"
                                                    background="#5F5D43"
                                                    cornerRadius="7"/>
                                            <Border width="78"
                                                    height="12"
                                                    horizontalAlignment="Left"
                                                    background="#4D4C36"
                                                    cornerRadius="6"/>
                                        </StackPanel>
                                        <ToggleSwitch gridColumn="1"
                                                      width="84"
                                                      height="48"
                                                      verticalAlignment="Center"
                                                      horizontalAlignment="Right"
                                                      background="#E5BD59"
                                                      borderBrush="#494938"
                                                      foreground="#F1E8D2"
                                                      tint="#A5A38C"
                                                      command="{Binding ToggleAlarmCommand}"
                                                      isOn="{Binding IsEnabled}">
                                            <ToggleSwitch.Storyboards>
                                                <Storyboard trigger="Toggled">
                                                    <FloatAnimation property="toggleProgress"
                                                                    from="Current"
                                                                    to="ToggleState"
                                                                    duration="220"
                                                                    easing="CubicOut"/>
                                                </Storyboard>
                                            </ToggleSwitch.Storyboards>
                                        </ToggleSwitch>
                                    </Grid>
                                </Grid>
                            </Grid>
                        </Border>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ScrollViewer>
</UserControl>