<Window x:Class="NavigationGraphLab.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Navigation graph lab"
        Width="1120"
        Height="760"
        MinWidth="760"
        MinHeight="520"
        Background="#202020"
        Foreground="#F2F2F2"
        WindowStartupLocation="CenterScreen">
  <Window.Resources>
    <SolidColorBrush x:Key="ControlBackgroundBrush"
                     Color="#303030" />
    <SolidColorBrush x:Key="ControlBorderBrush"
                     Color="#5A5A5A" />
    <SolidColorBrush x:Key="ControlForegroundBrush"
                     Color="#E6E6E6" />
    <Style TargetType="Button">
      <Setter Property="Padding"
              Value="10,5" />
      <Setter Property="Background"
              Value="{StaticResource ControlBackgroundBrush}" />
      <Setter Property="BorderBrush"
              Value="{StaticResource ControlBorderBrush}" />
      <Setter Property="BorderThickness"
              Value="1" />
      <Setter Property="Foreground"
              Value="{StaticResource ControlForegroundBrush}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Border"
                    Padding="{TemplateBinding Padding}"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver"
                       Value="True">
                <Setter TargetName="Border"
                        Property="Background"
                        Value="#464646" />
                <Setter TargetName="Border"
                        Property="BorderBrush"
                        Value="#D5BD7D" />
              </Trigger>
              <Trigger Property="IsPressed"
                       Value="True">
                <Setter TargetName="Border"
                        Property="Background"
                        Value="#5A4D26" />
                <Setter TargetName="Border"
                        Property="BorderBrush"
                        Value="#F0D78C" />
              </Trigger>
              <Trigger Property="IsKeyboardFocused"
                       Value="True">
                <Setter TargetName="Border"
                        Property="BorderBrush"
                        Value="#F0D78C" />
              </Trigger>
              <Trigger Property="IsEnabled"
                       Value="False">
                <Setter TargetName="Border"
                        Property="Opacity"
                        Value="0.45" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="ToolTip">
      <Setter Property="Background"
              Value="#252525" />
      <Setter Property="BorderBrush"
              Value="#4A4A4A" />
      <Setter Property="BorderThickness"
              Value="1" />
      <Setter Property="Foreground"
              Value="#E6E6E6" />
      <Setter Property="Padding"
              Value="6" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ToolTip">
            <Border Padding="{TemplateBinding Padding}"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
              <ContentPresenter />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal">
      <Button x:Name="OpenGraphButton"
              Padding="12,6"
              Content="Открыть JSON"
              Click="OpenGraphButtonClick" />
      <Button x:Name="ResetButton"
              Margin="8,0,0,0"
              Padding="12,6"
              Content="Сбросить выбор"
              Click="ResetButtonClick" />
      <TextBlock x:Name="GraphPathText"
                 Margin="16,0,0,0"
                 VerticalAlignment="Center"
                 Foreground="#BDBDBD" />
    </StackPanel>
    <TextBlock x:Name="StatusText"
               Grid.Row="1"
               Margin="0,12,0,12"
               TextWrapping="Wrap"
               Foreground="#D8D8D8" />
    <Border Grid.Row="2"
            Padding="1"
            BorderBrush="#515151"
            BorderThickness="1"
            Background="#292929">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="260" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ScrollViewer x:Name="BranchPanelScrollViewer"
                      Margin="8"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
          <StackPanel x:Name="BranchPanel"
                      Orientation="Vertical" />
        </ScrollViewer>
        <Viewbox Grid.Column="1"
                 Stretch="Uniform">
          <Canvas x:Name="GraphCanvas"
                  MinWidth="760"
                  MinHeight="480"
                  Background="#292929"
                  MouseLeftButtonDown="GraphCanvasMouseLeftButtonDown" />
        </Viewbox>
      </Grid>
    </Border>
  </Grid>
</Window>